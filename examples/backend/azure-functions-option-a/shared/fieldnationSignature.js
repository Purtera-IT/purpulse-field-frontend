/**
 * Re-exports the portable Field Nation verifier from `azure-function-api/shared/signature.js`
 * (same file to merge into **purpulse.app** `azure-function-api` when deploying).
 */
'use strict';

module.exports = require('../../../../azure-function-api/shared/signature');
